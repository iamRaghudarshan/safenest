# FinMate backend dependencies.
#
# Only direct dependencies are pinned; pip resolves the rest. Versions are the ones
# this app was built and tested against — loosen them only if you have a reason to.
#
# Install:  pip install -r requirements.txt

# --- web framework ---
fastapi==0.140.0
uvicorn[standard]==0.51.0
python-multipart==0.0.32

# --- database ---
SQLAlchemy==2.0.51
PyMySQL==1.2.0          # only used when FinMate is pointed at MySQL; SQLite is built in

# --- config ---
pydantic-settings==2.14.2
python-dotenv==1.2.2

# --- auth & crypto ---
# PyJWT, not python-jose: the latter requires `ecdsa`, which has an unfixed
# timing vulnerability (PYSEC-2026-1325) its maintainers will not address.
PyJWT==2.13.0
passlib==1.7.4
bcrypt==5.0.0
cryptography==49.0.0

# --- images (gallery, documents, scanning) ---
pillow==12.3.0
pillow-heif==1.5.0      # reads iPhone HEIC/HEIF photos

# --- push notifications ---
pywebpush==2.3.0
py-vapid==1.9.4

# --- outbound HTTP (CDN purge) ---
requests==2.34.2

# --- on-device image understanding (see app/vision.py) ---
# All CPU-only and offline. opencv supplies the YuNet/SFace face models' runtime;
# onnxruntime runs CLIP; transformers is used ONLY for CLIP's tokeniser, so torch
# is deliberately not installed — it would add ~2 GB for no benefit here.
numpy==2.5.1
opencv-python-headless==5.0.0.93
onnxruntime==1.28.0
transformers==5.14.1
